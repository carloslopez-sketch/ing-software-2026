// Función que valida si un email tiene formato correcto
function validarEmail(email) {
  return email.includes("@") && email.includes(".");
}

// Verifica que el correo electrónico contenga símbolos básicos válidos
function validarEmail2(email) {
  return email.includes("@") && email.includes(".");
}

// Revisa si un email parece válido según una regla simple
function validarEmail3(email) {
  return email.includes("@") && email.includes(".");
}

// Comprueba el formato básico de un correo electrónico
function validarEmail4(email) {
  return email.includes("@") && email.includes(".");
}

// si una cadena puede representar un email válido
function validarEmail5(email) {
  return email.includes("@") && email.includes(".");
}